# Состояние сервера: база, пользователи, ключ подписи токенов — НЕ публиковать
data/

# Локальные резервные копии и собранные архивы
backup/
dist/
*.zip

# Временные файлы тестов (создаются и удаляются tools/check.ps1)
tools/_*.js

# Зависимости (у проекта их нет, но на всякий случай)
node_modules/

# Системное
Thumbs.db
Desktop.ini
.DS_Store
